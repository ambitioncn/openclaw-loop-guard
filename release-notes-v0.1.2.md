# OpenClaw Loop Guard v0.1.2

ClawHub publishing retry with the current ClawHub CLI.

## Changes

- Publishes the same runtime behavior as v0.1.0/v0.1.1.
- Uses the updated ClawHub package publishing path after the older local CLI produced an uninspectable v0.1.1 registry version.

## Verification

- `npm test` passed: 29/29 tests.
- `npm run check` passed.

