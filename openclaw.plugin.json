{
  "id": "loop-guard",
  "name": "Loop Guard",
  "description": "Detects repeated OpenClaw tool failures and blocks unproductive retry loops.",
  "contracts": {
    "agentToolResultMiddleware": [
      "openclaw",
      "codex"
    ]
  },
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "blockRepeatedCalls": {
        "type": "boolean",
        "default": false
      },
      "blockAfterPendingTimeout": {
        "type": "boolean",
        "default": true
      },
      "pendingTimeoutMs": {
        "type": "integer",
        "minimum": 0,
        "default": 180000
      },
      "driverModel": {
        "type": "string",
        "description": "Model acting as the primary reasoning driver, for example qwen-local/qwen36-uncensored-llamacpp."
      },
      "executorModel": {
        "type": "string",
        "description": "Higher-capability model recommended for tool execution escalation, for example openai/gpt-5.5 or anthropic/claude-sonnet."
      },
      "executorRuntime": {
        "type": "string",
        "default": "codex"
      },
      "handoffEnabled": {
        "type": "boolean",
        "default": false
      },
      "handoffOnSoftWarn": {
        "type": "boolean",
        "default": true
      },
      "handoffOnBlock": {
        "type": "boolean",
        "default": true
      },
      "handoffSessionPrefix": {
        "type": "string",
        "default": "loop-guard"
      },
      "handoffToolsAllow": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "Tool allow-list passed to the plugin-owned handoff subagent. Empty means no tools are exposed."
      },
      "approvedHandoffToolsAllow": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [
          "read",
          "exec",
          "bash",
          "apply_patch"
        ],
        "description": "Default tool allow-list used when a human runs /loop-guard approve latest."
      },
      "approvedHandoffWriteRoots": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "Filesystem roots the approved handoff executor may write under. Empty means no writes are approved."
      },
      "approvedHandoffAllowRiskyOperations": {
        "type": "boolean",
        "default": true,
        "description": "When true, a human approve command is enough to authorize configured risky operation patterns for the approved handoff."
      },
      "approvedHandoffMaxAgeMs": {
        "type": "integer",
        "minimum": 0,
        "default": 1800000,
        "description": "Maximum age of a pending handoff that /loop-guard approve may resume. Set 0 to disable age checks."
      },
      "approvedHandoffRequireExecTimeout": {
        "type": "boolean",
        "default": true,
        "description": "Require approved executor prompts to use explicit timeouts or bounded guards for exec commands."
      },
      "approvedHandoffRiskyPatterns": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [
          "ssh",
          "scp",
          "rsync",
          "systemctl",
          "service",
          "sudo",
          "rm",
          "mv",
          "git push",
          "npm publish",
          "openclaw update",
          "models auth",
          "secrets"
        ],
        "description": "Operation patterns that require separate approval unless the approve command includes confirm=risky."
      },
      "paramsPreviewMaxChars": {
        "type": "integer",
        "minimum": 0,
        "default": 1000,
        "description": "Maximum length of the redacted tool-argument preview included in audit events and handoff prompts; 0 disables previews."
      },
      "softThreshold": {
        "type": "integer",
        "minimum": 1,
        "default": 2
      },
      "hardThreshold": {
        "type": "integer",
        "minimum": 1,
        "default": 3
      },
      "windowMs": {
        "type": "integer",
        "minimum": 1000,
        "default": 600000
      },
      "maxEntries": {
        "type": "integer",
        "minimum": 10,
        "default": 500
      },
      "statePath": {
        "type": "string"
      },
      "highRiskTools": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [
          "exec",
          "bash",
          "apply_patch",
          "write",
          "edit"
        ]
      },
      "softMessage": {
        "type": "string"
      },
      "hardMessage": {
        "type": "string"
      },
      "pendingMessage": {
        "type": "string"
      }
    }
  }
}
