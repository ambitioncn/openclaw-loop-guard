# OpenClaw Loop Guard v0.1.1

Packaging-only stable release for ClawHub.

## Changes

- Adds required `openclaw.build.openclawVersion` and `openclaw.build.pluginSdkVersion` metadata for external ClawHub code-plugin publishing.
- Keeps the v0.1.0 runtime behavior unchanged.

## Verification

- `npm test` passed: 29/29 tests.
- `npm run check` passed.

